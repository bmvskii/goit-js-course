document.addEventListener('DOMContentLoaded', () => {

    // const changeTitle = (event) => {
    //     const { currentTarget } = event;

    //     const card = currentTarget.parentElement;
        
    // }   

    // const cards = document.querySelectorAll('.card');
    // cards.forEach(card => {
    //     const button = card.querySelector('.card__button');
    //     button.addEventListener('click', changeTitle);
    // });

    // const wrapper = document.querySelector('.wrapper');

    // wrapper.addEventListener('click', (event) => {
    //     const { target, currentTarget } = event;

    //     console.log(target);
    //     console.log(currentTarget);
        
    //     if (target.classList.contains('card__button')) {
    //         const card = target.parentElement;
    //         const title = card.querySelector('.card__title');

    //         title.textContent = "New title";
        
    //     } else if (target.classList.contains('card')) {
    //         target.style.backgroundColor = 'yellow';
    //     }

    // });

    // document.body.addEventListener('click', () => console.log("Body click"));
    // document.querySelector('.wrapper').addEventListener('click', () => console.log("Wrapper click"));
    // document.querySelector('.card').addEventListener('click', (event) => {
    //     console.log("Card click 1");
        
    //     event.stopPropagation();
    //     event.stopImmediatePropagation();
    // });
    // document.querySelector('.card').addEventListener('click', (event) => {
    //     console.log("Card click 2");
    // });
    // document.querySelector('.card__button').addEventListener('click', () => console.log("Button click"));
});